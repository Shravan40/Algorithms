#include <iostream>
#include <vector>

using namespace std;


int max_sub_array(vector<int> data,int low,int high)
{
    if(high == low)
        return (data[low]);
    else
    {
        int mid = (low + high) / 2;

    }
}

int main()
{
    ios_base::sync_with_stdio(false);
    vector<int> data;
    for(int )
    return 0;
}
